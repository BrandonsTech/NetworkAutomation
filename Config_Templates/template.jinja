{% for interface in interfaces_list %}
interface {{ interface.name }}
    description {{ interface.description -}}
    {% if interface.uplink == True %}
    switchport mode trunk
    {% else %}
    switchport mode access
    switchport access vlan {{ interface.vlan -}}
    {% endif %}
{% endfor %}